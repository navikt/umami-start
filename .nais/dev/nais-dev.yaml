apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: umami-start
  namespace: team-researchops
  labels:
    team: team-researchops
spec:
  image: {{ image }}
  port: 8080
  envFrom:
  - secret: umami-bigquery
  liveness:
    path: /isalive
    port: 8080
    initialDelay: 20
    timeout: 60
  readiness:
    path: /isready
    port: 8080
    initialDelay: 20
    timeout: 60
  replicas:
    min: 1
    max: 1
  resources:
    limits:
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi
  ingresses:
    - https://startumami.ansatt.dev.nav.no
  accessPolicy:
    outbound:
      rules:
          - application: reops-proxy
            namespace: team-researchops
          - application: start-umami-backend
            namespace: team-researchops
            namespace: team-researchops
  azure:
    application:
      enabled: true
      allowAllUsers: true
      tenant: nav.no # bytte til trygdeetaten.no ?
    sidecar:
      autoLogin: true
      enabled: true
  env:
    - name: SITEIMPROVE_BASE_URL
      value: "http://reops-proxy.team-researchops.svc.cluster.local/siteimprove"
    - name: UMAMI_BASE_URL
      value: "https://reops-event-proxy.ekstern.dev.nav.no"
    - name: GCP_PROJECT_ID
      value: "team-researchops-dev-4396"
    - name: BACKEND_BASE_URL
      value: "http://start-umami-backend"
    - name: BACKEND_OBO_SCOPE
      value: "api://dev-gcp.team-researchops.start-umami-backend/.default"
